name: EAS Update

on:
  push:
    branches:
      - main

jobs:
  update:
    name: Publish Over-The-Air Update
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 20.x 
          cache: npm

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          packager: npm
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm install

      - name: Publish update to Expo
        # Added the message flag so the server doesn't wait for human input!
        run: eas update --branch preview --message "Automated update from GitHub Actions"